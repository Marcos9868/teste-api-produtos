const Product = (uuid, name, description, price, quantity) => {
  this.uuid = uuid,
  this.name = name,
  this.description = description,
  this.price = price,
  this.quantity = quantity
}

module.exports = Product